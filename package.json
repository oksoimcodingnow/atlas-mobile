{
  "name": "atlas-mobile",
  "version": "0.0.1",
  "description": "ATLAS Mobile — AI coding agent that lives in your phone's home screen",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "@octokit/rest": "^21.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
